<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">점호</a>

    <div class="collapse navbar-collapse show" id="navbarColor03">
      <ul class="navbar-nav me-auto">

        <li class="nav-item">
          <a class="nav-link active" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a id="join" class="nav-link" href="/join">회원가입</a>
        </li>
        {% if user %}
        <li class="nav-item">
          <a id="my-profile" class="nav-link" href="/users/{{user.id}}">내 정보 수정</a>
        </li>
        {% endif %}
      </ul>
      {% if user %}
      <div class="d-flex">
        <a id="logout" class="btn btn-warning" href="/auth/logout">로그아웃</a>
      </div>
      {% endif %}
    </div>
  </div>
</nav>